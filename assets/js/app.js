var app = app || {};app.init_toc = function() {    var $post = $('.post_content');    if (!$post[0]) {        return;    }    $post.toc({        showAlways: true    });    var $toc = $('#toc-container');    if (!$toc[0]) {        return;    }    var margin = 20;    var tt = $toc.offset().top-margin;    $(window).scroll(function() {        if (!$toc[0]) {            return;        }        if (tt < app.w.scrollTop()) {            $toc.css({position: 'fixed', top: margin});        } else {            $toc.css({position: '', top: ''});        }    });};$(function () {    app.w = $(window);    app.init_toc();});